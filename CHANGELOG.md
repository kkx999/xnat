# Changelog

## v1.1.0

- 新增独立公告中心：用户右上角历史公告入口、未读数量、首次登录重点公告，以及后台独立公告管理。
- 公告支持多条历史记录、草稿/发布/下线、置顶；修改标题或正文后会重置该公告已读状态。
- 旧版单条站点公告自动迁移到公告中心，并尽可能继承旧用户已读状态，避免无意义重复弹出。

- 新增宿主机维护 / Drain 模式：停止新 VPS 调度，不影响现有实例运行与管理。
- 新增 CPU、内存、natpool 存储调度水位；达到阈值时自动拒绝新开通并显示具体原因。
- 新增 Host Agent 离线 / 恢复、natpool 水位、NAT 端口余量、任务最终失败、数据库备份失败的管理员通知。
- 新增 VPS 到期生命周期：提前提醒、宽限期、自动停机、续费自动恢复，以及默认关闭的延迟自动删除。
- 自动删除默认关闭；开启后仍经过宽限期、删除等待期和最终提醒，不会因升级立即删除实例。
- 流量周期与 VPS 到期时间彻底分离，支持每 30 天滚动或每月固定日期重置。
- 用户端新增轻量卡片反馈、按钮处理中状态、复制反馈、Toast、折叠动画和流量进度过渡；保持深蓝商业 IDC 风格。
- 本轮将用户端 Flash 提示收紧为右上角 300px 浮动 Toast 小卡片，加入成功/失败/警告状态图标，并修复提示占位和页面遮挡感。
- 新增 v1.1.0 additive SQLite schema migration；可从 v1.0.x 数据库自动补齐新字段，旧数据库备份仍可恢复。
- 新增并验收 v1.0.2 → v1.1.0 原地升级路径：升级前自动备份数据库/.env/代码/服务，保留现有数据与监听配置，失败时自动回滚。
- Panel 升级至 v1.1.0；Host Agent 保持 v1.0.0，Agent API 保持 v1。

## v1.0.2

- 手动补单新增“无 TxHash 强制补单”高级模式：不伪造交易哈希、不写链上交易表，要求 FORCE CREDIT 与补单原因。
- 无 TxHash 补单仍以订单为幂等边界，同一订单只能入账一次，并完整记录余额流水与审计日志。

- 新充值订单统一生成 3 位小数的 USDT 精确金额；旧未支付订单保留原始精确金额，避免升级后付款金额变化。
- 充值后台新增手动补单：TxHash 链上校验、全局防重复、PAID 确认入账。
- 自动校验不可用时支持 FORCE PAID 强制补单，并写入完整审计日志。
- 补单始终按订单原人民币金额入账，不按补单当天汇率重新计算。
- 补单面板采用对称栅格布局，表格操作按钮、输入框与确认按钮统一尺寸。

- 重构用户 USDT 充值订单卡片，突出支付金额、网络、收款地址和支付期限。
- 用户充值页面不再显示 Token 合约地址，避免普通用户误把合约地址当作收款地址。
- 收款地址和精确金额新增统一复制按钮与“已复制”反馈。
- 链上状态与交易哈希展示重新整理，减少支付页面的信息干扰。
- 支付风险提示改为紧凑提示条，与客户端深蓝主题保持一致。
- 保留后台 USDT 官方合约只读展示与自动/人工充值模式。
- Panel 升级至 v1.0.2；Host Agent 保持 v1.0.0，Agent API 保持 v1。

## v1.0.1

- 管理后台左侧导航改为统一视觉的折叠分类，当前分类自动展开。
- 后台整体配色统一为深蓝灰体系，移除旧紫色选中态并提升文字对比度。
- 任务队列、套餐与库存、用户管理、宿主机节点、表格、输入框与操作按钮统一 UI。
- 数据库备份页新增本地 `.db` 上传、SQLite 完整性与结构校验。
- 服务器已有备份支持直接从后台恢复。
- 恢复前自动创建 `pre-restore` 安全快照，恢复失败自动回滚。
- 数据库上传、下载、恢复操作写入审计日志。
- 数据库备份 / 恢复页面按钮、文件选择器、危险确认区域重新统一视觉。
- 修复较矮桌面视口中侧边栏底部入口不易访问的问题。
- 修复 `xnat update` 在 `pipefail` 环境下解析 GitHub 源码包时可能提前退出的问题。
- USDT 充值支持自动扫链 / 人工确认两种模式。
- TRON USDT 与 Polygon USDT0 合约地址改为系统只读常量，后台不接受自定义合约。
- 新增 TRON / Polygon 通道配置测试；测试不会发送资产。
- 人工充值支持用户提交 TxHash，并由管理员核对后人工确认入账。
- Host Agent 保持 v1.0.0，Agent API 保持 v1。

## v1.0.0

XNAT 首个正式版本。

- Panel Server + Host Agent 多节点架构
- Incus + LVM Thin NAT VPS
- Host 交互式安装：Panel 公网 IP + natpool
- NAT 端口池改由 Panel 后台在节点接入后配置
- NAT 端口池自动同步 Agent，并由 Agent 二次校验
- 节点端口池使用量 / 剩余量统计
- Panel 域名、Nginx、Let's Encrypt、Cloudflare
- Cloudflare 真实 IP 与源站防绕过
- XNAT 敏感管理端口防火墙
- `xnat` Panel / Host 统一管理菜单
- 组件独立版本更新与 Agent API 兼容机制
- 数据库备份 / 恢复
- Agent Token 轮换
- 系统诊断
- VPS 流量额度可直接调整，并支持重置当前流量周期
- VPS 到期时间支持自定义续期天数或直接指定日期时间
- 磁盘缩容前端提示与后端双重保护
- USDT 自动充值从站点设置独立为业务页面
- 通知服务从站点设置独立，支持 SMTP / Telegram 测试、全站通知规则与发送记录
- Multi-Node NAT 端口占用按 Host + 协议隔离，不再使用全局端口唯一约束
