# Changelog

## v1.3.2

- 正式集成 XNAT Mobile API v1，为 XNAT Android v1.0.0 提供稳定的 `/api/v1` 接口。
- 新增 Bearer Token 登录/退出、账户信息、概览、服务器列表和详情接口；沿用既有登录失败封禁、TOTP、LoginSession 与审计逻辑。
- Android 可执行 VPS 开机、关机、重启，管理 NAT TCP/UDP 端口映射，并通过既有任务队列执行系统重装。
- 新增移动端账务读取、工单列表/创建/详情/回复/关闭接口。
- 新增套餐目录、优惠码报价、余额购买和自动开通接口；复用 Web Panel 的套餐、库存、调度、余额和订单业务规则。
- 购买接口使用 `request_id` 幂等保护，网络重试不会重复扣款、重复生成订单或重复开通 VPS。
- Mobile API v1 复用现有数据库表，不引入破坏性 schema migration；浏览器 Web Panel 的 Session + CSRF 流程保持不变。
- `/api/v1/health` 继续保持 API v1，并增加当前 Panel 版本字段，便于客户端兼容性诊断。
- 正式验收 v1.3.1 → v1.3.2 原地升级路径；升级前自动 `quick_check` + 完整备份，失败时沿用自动回滚。
- 兼容已经在 v1.3.1 上部署 Mobile API dev1～dev5 的实例，升级后统一收敛为正式 Mobile API v1。
- Panel 升级至 v1.3.2；Host Agent 保持 v1.1.0，Agent API 保持 v1。

## v1.3.1

- 移动端用户中心侧边栏改为真正的 off-canvas Drawer，桌面端继续保持完整展开布局。
- 移动端导航按“概览 / 服务 / 账务 / 支持 / 账户 / 管理”分类独立折叠；当前页面分类自动展开，并在当前标签页记忆展开状态。
- 支持遮罩、关闭按钮、ESC、点击导航项和 Drawer 内左滑关闭；打开时锁定背景滚动，并补充 `aria-hidden` / `inert` 可访问性控制。
- 修复透明 backdrop 在关闭状态仍可能覆盖页面并拦截全部点击的问题。
- 修复移动端 hamburger 三条横线未对齐。
- 使用 `visualViewport.height` + `100svh` fallback 计算 Drawer 实际可视高度，并增加 Android gesture/navigation bar bottom guard。
- 中间导航区域独立滚动，底部用户信息、余额、首页和退出始终保留在可视区域。
- 深色 / 明亮主题同步适配移动端 Drawer。
- `scripts/check.sh` 自动优先复用已安装 Panel 的 `.venv`（含 Jinja2），并新增移动端导航回归守卫。
- 正式支持 v1.3.0 → v1.3.1 Panel 原地升级；继续支持 v1.2.0 → v1.3.1 additive schema 直接升级。
- Panel 升级至 v1.3.1；Host Agent 保持 v1.1.0，Agent API 保持 v1。

## v1.3.0

- 发布前完整性补强：后台 KVM 套餐与实例资源限制采用 512MB / 4GB 前后端双校验。
- KVM 重置 root 密码会等待 Guest Agent Ready，避免刚开机/重启后的瞬时失败。
- 全量状态校验新增 LXC/KVM 类型与 CPU/内存/磁盘配置漂移检查。
- 用户服务器列表/控制台补充 LXC/KVM 标签；后台手动开通与节点套餐绑定补充虚拟化类型提示。
- 危险操作文案由“容器内数据”统一为“实例内数据”。


- 发布前 UI 收尾：用户端服务器卡片与套餐卡片改为自适应占满可用宽度，减少宽屏右侧大面积留白。
- 套餐购买恢复并改为始终显示“优惠码（可选）”输入框，不再藏在折叠区。
- 管理后台“套餐与库存”改为逐套餐折叠编辑，收起状态直接展示规格、虚拟化类型、售价和库存，显著减少页面纵向占用。
- 真实 Host KVM 链路验证：Incus VM、DHCP/出网、incus-agent、root SSH、proxy NAT 公网端口转发均通过。
- 修正 KVM 客户机网卡名不一定为 `eth0` 的兼容问题，并强化 SSH 密码登录配置优先级校验。

- 新增 Hybrid Virtualization Host：安装时可选择 LXC、KVM 或 LXC + KVM。
- 自动检测 `/dev/kvm` 与 Nested Virtualization 可用性，并在 KVM 模式进行临时 VM 验证。
- Host Agent 升级至 v1.1.0，状态接口上报虚拟化模式和 KVM 能力。
- 套餐和 Server 新增虚拟化类型字段；调度器强制执行 LXC/KVM 能力匹配。
- KVM 实例使用 Incus `--vm` 创建；LXC 创建路径保持兼容。
- KVM 创建流程显式等待 Guest Agent 就绪，避免 VM 已 RUNNING 但 `incus exec` 尚不可用导致开通失败。
- 修复 SSH 校验在 `set -o pipefail` 下因 `grep -q` 提前退出触发 SIGPIPE（exit 141）而误判开通失败。
- Host Agent 命令错误改为保留 exit code、有效 stderr/stdout，并过滤 apt-utils 的无害 debconf 警告。
- 用户端“我的服务器”和“套餐中心”统一为标准响应式网格：宽屏 3 列、中屏 2 列、移动端 1 列；最后一行保持左对齐，不再拉伸或居中。
- 支持 v1.2.0 → v1.3.0 additive schema 原地升级。

## v1.2.0

- 用户前端新增深色 / 柔和明亮主题切换，并在浏览器中记忆用户选择；全面修正明亮主题中的暗色残留与低对比文字。
- 管理后台新增独立深色 / 明亮主题切换，统一侧边栏、表格、表单、服务器、宿主机、支付、公告、备份和系统设置等页面的明亮模式。
- 重构后台服务器管理 UI：资源、SSH、带宽、流量、到期和操作区域改为更紧凑、清晰的卡片与折叠交互，统一按钮尺寸和对齐。
- 重构宿主机卡片：缩小整体尺寸，统一字体层级，显示 CPU 调度余量、剩余可分配内存/存储及已分配配置；“可调度”文案改为“允许分配”。
- 宿主机允许套餐改为 iOS 风格 Switch；修复保存/检测等操作后“添加宿主机节点”自动展开的问题。
- 统一前后台复制按钮、Toast、Switch、折叠区和按钮交互，修复复制按钮点击时因 transform 冲突造成的大幅跳动。
- VPS 重装状态改为任务期间显示“重装中”，任务完成后自动恢复“运行中”，避免重装过程中仍显示绿色运行状态。
- 套餐购买优惠码区域重新设计为可感知的展开式输入区，并修复展开后内容裁切与卡片高度不足。
- 套餐后台表单改为统一 Grid 基线，输入框、下拉框、帮助文字和操作按钮对齐；流量重置价格作为套餐独立配置项。
- 新增用户付费自助流量重置：仅流量耗尽时显示，按套餐配置价格从余额扣费，生成订单和余额流水，开启新流量周期并恢复套餐带宽。
- 既有套餐在数据库迁移时自动以月付价格回填默认流量重置价格；迁移保持 additive，不重建旧表。
- 新增统一 XNAT 网页确认 Modal：支持费用/余额/扣费后余额、操作结果提示、遮罩关闭、ESC 和移动端适配；移除浏览器原生 `confirm()` 交互。
- 继续保留 v1.1.1 的公告中心、历史公告、首次登录重点公告、永久删除、3 秒 Toast 等能力，并进一步统一亮色模式样式。
- 正式验收 v1.1.1 → v1.2.0 原地升级路径；升级前自动备份并保留 `.env`、SQLite、用户、余额、订单、VPS、Host、套餐、支付、通知和公告数据，失败时自动尝试回滚。
- Panel 升级至 v1.2.0；Host Agent 保持 v1.0.0，Agent API 保持 v1。

## v1.1.1

- 优化后台公告中心布局，移除长期占位的公告规则与冗余说明，集中展示公告编辑与历史公告。
- 统一公告中心输入框、下拉框、创建/搜索/保存/删除等操作按钮的高度、间距与对齐。
- “置顶显示”和“首次登录重点弹出”改为清晰可见的现代 Switch 交互，并统一编辑态样式。
- 公告不再使用“下线公告”作为删除替代；新增永久删除公告能力，并同步清理该公告的已读记录与写入审计日志。
- 后台 Flash 提示与用户前端统一为右上角浮动 Toast，默认 3 秒自动消失，悬停暂停计时，点击可立即关闭。
- 更新静态资源缓存版本，避免升级后浏览器继续加载旧公告中心样式。
- 正式验收 v1.1.0 → v1.1.1 原地升级路径；升级前自动备份并完整保留 `.env`、SQLite、用户、余额、订单、VPS、Host、支付、通知和公告数据。
- Panel 升级至 v1.1.1；Host Agent 保持 v1.0.0，Agent API 保持 v1。

## v1.1.0

- 新增独立公告中心：用户右上角历史公告入口、未读数量、首次登录重点公告，以及后台独立公告管理。
- 公告支持多条历史记录、草稿/发布/下线、置顶；修改标题或正文后会重置该公告已读状态。
- 旧版单条站点公告自动迁移到公告中心，并尽可能继承旧用户已读状态，避免无意义重复弹出。

- 新增宿主机维护 / Drain 模式：停止新 VPS 调度，不影响现有实例运行与管理。
- 新增 CPU、内存、natpool 存储调度水位；达到阈值时自动拒绝新开通并显示具体原因。
- 新增 Host Agent 离线 / 恢复、natpool 水位、NAT 端口余量、任务最终失败、数据库备份失败的管理员通知。
- 新增 VPS 到期生命周期：提前提醒、宽限期、自动停机、续费自动恢复，以及默认关闭的延迟自动删除。
- 自动删除默认关闭；开启后仍经过宽限期、删除等待期和最终提醒，不会因升级立即删除实例。
- 流量周期与 VPS 到期时间彻底分离，支持每 30 天滚动或每月固定日期重置。
- 用户端新增轻量卡片反馈、按钮处理中状态、复制反馈、Toast、折叠动画和流量进度过渡；保持深蓝商业 IDC 风格。
- 本轮将用户端 Flash 提示收紧为右上角 300px 浮动 Toast 小卡片，加入成功/失败/警告状态图标，并修复提示占位和页面遮挡感。
- 新增 v1.1.0 additive SQLite schema migration；可从 v1.0.x 数据库自动补齐新字段，旧数据库备份仍可恢复。
- 新增并验收 v1.0.2 → v1.1.0 原地升级路径：升级前自动备份数据库/.env/代码/服务，保留现有数据与监听配置，失败时自动回滚。
- Panel 升级至 v1.1.0；Host Agent 保持 v1.0.0，Agent API 保持 v1。

## v1.0.2

- 手动补单新增“无 TxHash 强制补单”高级模式：不伪造交易哈希、不写链上交易表，要求 FORCE CREDIT 与补单原因。
- 无 TxHash 补单仍以订单为幂等边界，同一订单只能入账一次，并完整记录余额流水与审计日志。

- 新充值订单统一生成 3 位小数的 USDT 精确金额；旧未支付订单保留原始精确金额，避免升级后付款金额变化。
- 充值后台新增手动补单：TxHash 链上校验、全局防重复、PAID 确认入账。
- 自动校验不可用时支持 FORCE PAID 强制补单，并写入完整审计日志。
- 补单始终按订单原人民币金额入账，不按补单当天汇率重新计算。
- 补单面板采用对称栅格布局，表格操作按钮、输入框与确认按钮统一尺寸。

- 重构用户 USDT 充值订单卡片，突出支付金额、网络、收款地址和支付期限。
- 用户充值页面不再显示 Token 合约地址，避免普通用户误把合约地址当作收款地址。
- 收款地址和精确金额新增统一复制按钮与“已复制”反馈。
- 链上状态与交易哈希展示重新整理，减少支付页面的信息干扰。
- 支付风险提示改为紧凑提示条，与客户端深蓝主题保持一致。
- 保留后台 USDT 官方合约只读展示与自动/人工充值模式。
- Panel 升级至 v1.0.2；Host Agent 保持 v1.0.0，Agent API 保持 v1。

## v1.0.1

- 管理后台左侧导航改为统一视觉的折叠分类，当前分类自动展开。
- 后台整体配色统一为深蓝灰体系，移除旧紫色选中态并提升文字对比度。
- 任务队列、套餐与库存、用户管理、宿主机节点、表格、输入框与操作按钮统一 UI。
- 数据库备份页新增本地 `.db` 上传、SQLite 完整性与结构校验。
- 服务器已有备份支持直接从后台恢复。
- 恢复前自动创建 `pre-restore` 安全快照，恢复失败自动回滚。
- 数据库上传、下载、恢复操作写入审计日志。
- 数据库备份 / 恢复页面按钮、文件选择器、危险确认区域重新统一视觉。
- 修复较矮桌面视口中侧边栏底部入口不易访问的问题。
- 修复 `xnat update` 在 `pipefail` 环境下解析 GitHub 源码包时可能提前退出的问题。
- USDT 充值支持自动扫链 / 人工确认两种模式。
- TRON USDT 与 Polygon USDT0 合约地址改为系统只读常量，后台不接受自定义合约。
- 新增 TRON / Polygon 通道配置测试；测试不会发送资产。
- 人工充值支持用户提交 TxHash，并由管理员核对后人工确认入账。
- Host Agent 保持 v1.0.0，Agent API 保持 v1。

## v1.0.0

XNAT 首个正式版本。

- Panel Server + Host Agent 多节点架构
- Incus + LVM Thin NAT VPS
- Host 交互式安装：Panel 公网 IP + natpool
- NAT 端口池改由 Panel 后台在节点接入后配置
- NAT 端口池自动同步 Agent，并由 Agent 二次校验
- 节点端口池使用量 / 剩余量统计
- Panel 域名、Nginx、Let's Encrypt、Cloudflare
- Cloudflare 真实 IP 与源站防绕过
- XNAT 敏感管理端口防火墙
- `xnat` Panel / Host 统一管理菜单
- 组件独立版本更新与 Agent API 兼容机制
- 数据库备份 / 恢复
- Agent Token 轮换
- 系统诊断
- VPS 流量额度可直接调整，并支持重置当前流量周期
- VPS 到期时间支持自定义续期天数或直接指定日期时间
- 磁盘缩容前端提示与后端双重保护
- USDT 自动充值从站点设置独立为业务页面
- 通知服务从站点设置独立，支持 SMTP / Telegram 测试、全站通知规则与发送记录
- Multi-Node NAT 端口占用按 Host + 协议隔离，不再使用全局端口唯一约束
