"""XNAT business-service layer.

Reusable validation and state-transition rules live here so Web, Mobile API and
background workers share the same behavior without changing the UI layer.
"""

# Release 1.0.2 keeps presentation concerns outside this package.
